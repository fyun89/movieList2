var AddToList = (props) => {
	return (
		<form id="addInput">
			<label>
				Add New Movie:
				<input type="text" placeholder="Add a movie" />
			</label>
				<input type="submit" value="Add" />
		</form>
	)
}

window.AddToList.AddToList